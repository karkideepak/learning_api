User-agent: * → Applies to all search engine crawlers
Disallow: /admin → Blocks crawlers from /admin
Allow: / → Allows access to the rest of the site
Sitemap → Tells crawlers where the sitemap is located
